const path = require('path')

const env = process.env.NODE_ENV || 'development'
const config = require('./api-server/config/config')[env]

module.exports = {
  'config': path.join('api', 'config', 'config.asd'),
  'migrations-path': path.join('db', 'migrate'),
  'seeders-path': path.join('db', 'seed'),
  'models-path': path.join('db', 'models'),
}
