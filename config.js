const path = require('path')

module.exports = {
  viewsPath: path.join('web-server', 'views'),
  sharedPath: path.join('web-server', 'views', 'shared'),
  publicPath: path.join('web-server', 'public')
}
